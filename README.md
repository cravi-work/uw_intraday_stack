# UW Intraday Stack

High-reliability financial data ingestion system and institutional capability mapper.

## Quickstart (Windows `cmd.exe`)

This system is optimized for deterministic Windows environments. Open `cmd.exe` (Command Prompt, **not** PowerShell) and run:

1. **Create and activate the virtual environment**
```cmd
python -m venv .venv
.venv\Scripts\activate.bat
pip install -r requirements.txt